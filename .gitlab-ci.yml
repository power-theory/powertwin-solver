stages:
  - build
  - deploy

image: docker/compose:2.13.0

build:
  stage: build
  script:
    - echo "Building Docker images..."
    - docker-compose -f docker-compose.yml build

deploy:
  stage: deploy
  script:
    - echo "Deploying Docker containers..."
    - docker-compose -f docker-compose.yml up -d